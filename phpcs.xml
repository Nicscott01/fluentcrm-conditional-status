<?xml version="1.0"?>
<ruleset name="FluentCRM Conditional Status">
	<description>WordPress Coding Standards for FluentCRM Conditional Status</description>

	<!-- What to scan -->
	<file>.</file>

	<!-- Exclude patterns -->
	<exclude-pattern>/vendor/*</exclude-pattern>
	<exclude-pattern>/node_modules/*</exclude-pattern>
	<exclude-pattern>/assets/*</exclude-pattern>
	<exclude-pattern>*.js</exclude-pattern>
	<exclude-pattern>*.css</exclude-pattern>

	<!-- Show progress and sniff codes -->
	<arg value="ps"/>

	<!-- Strip the filepaths down to the relevant bit -->
	<arg name="basepath" value="."/>

	<!-- Check up to 8 files simultaneously -->
	<arg name="parallel" value="8"/>

	<!-- Use colors in output -->
	<arg name="colors"/>

	<!-- Include the WordPress-Core standard -->
	<rule ref="WordPress-Core">
		<!-- Allow array short syntax -->
		<exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
	</rule>

	<!-- Include the WordPress-Docs standard -->
	<rule ref="WordPress-Docs"/>

	<!-- Include the WordPress-Extra standard -->
	<rule ref="WordPress-Extra">
		<!-- Exclude filename rules as we use class- prefix -->
		<exclude name="WordPress.Files.FileName.InvalidClassFileName"/>
		<exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
	</rule>

	<!-- Add in some extra rules from other standards -->
	<rule ref="Generic.CodeAnalysis.UnusedFunctionParameter"/>
	<rule ref="Generic.Commenting.Todo"/>

	<!-- Check for PHP cross-version compatibility -->
	<config name="testVersion" value="7.4-"/>
	<rule ref="PHPCompatibilityWP"/>

	<!-- WordPress specific rules -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="fluentcrm-conditional-status"/>
			</property>
		</properties>
	</rule>

	<!-- Set minimum supported WordPress version -->
	<config name="minimum_supported_wp_version" value="5.8"/>

	<!-- Enforce PSR-4 autoloading -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="FluentCRM_Conditional_Status"/>
				<element value="fluentcrm_conditional_status"/>
				<element value="FLUENTCRM_CONDITIONAL_STATUS"/>
			</property>
		</properties>
	</rule>

	<!-- Allow for theme specific exceptions to the WordPress standards -->
	<rule ref="WordPress.Security.EscapeOutput">
		<properties>
			<property name="customAutoEscapedFunctions" type="array">
				<element value="__"/>
			</property>
		</properties>
	</rule>
</ruleset>
